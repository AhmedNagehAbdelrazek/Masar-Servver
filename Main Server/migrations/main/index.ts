import { runMigrations, getVersionsDir } from './runner';

export { runMigrations, getVersionsDir };
export default { runMigrations, getVersionsDir };
module.exports = { runMigrations, getVersionsDir };
