import { runMigrations } from './main/runner';
import { initMigrations } from './scripts/init';

export { runMigrations, initMigrations };
export default { runMigrations, initMigrations };
module.exports = { runMigrations, initMigrations };
